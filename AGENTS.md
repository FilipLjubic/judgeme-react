<!-- ctx:start -->

## ctx

Use `ctx` for this project's local context and operational memory.

Before non-trivial work, check for prior project lessons:

- `ctx recall "<task, repo, or failure pattern>" --cwd <repo>` recalls scoped memories. Treat results as evidence-backed hints and verify drift-prone facts against the live repo.
- `ctx hook recall "<latest user turn or task>" --cwd <repo>` returns bounded prompt-ready memory context. Inject it only as supporting context; live repo evidence still wins.
- `ctx remember "<concise reusable lesson>" --kind preference|fact|decision|recipe|warning --subject <stable.topic> --scope project --cwd <repo>` stores confirmed durable lessons. Use `--suggested` for plausible but unconfirmed lessons. Do not store secrets, one-off noise, or unresolved guesses.
- Before final responses on non-trivial work, run a visible memory writeback check. If the turn established a durable preference, decision, repo workflow, root cause, verification rule, or reusable lesson, call `ctx remember` in the active conversation. Skip current/latest facts unless they are stored as dated observations with date and source.
- `ctx memory review --cwd <repo>`, `ctx memory accept <id> --cwd <repo>`, and `ctx memory reject <id> --cwd <repo>` manage review-gated memory candidates.
- `ctx memory process --cwd <repo>` claims queued memory work for the visible agent harness. Do not run hidden background model work; apply results explicitly with `ctx memory job apply <id> <result.json> --cwd <repo>`.
- `ctx offload add --kind <kind> --title <title> --content-file <path> --cwd <repo>` stores large payloads as blobs; `ctx offload graph --cwd <repo>` renders the task graph as Mermaid.

Use project context when source, docs, research, or notes evidence is needed:

- `ctx query "<question>" --cwd <repo>` searches project docs, research papers, and notes.
- `ctx query "<question>" --debug --cwd <repo>` includes ranking and section details.
- `ctx path <label>` prints the local path for pinned source repos.
- `ctx show` inspects the project manifest.
- `ctx list --project` shows linked resources.

Source repos are explored on disk. Docs, research papers, and notes are returned as cited context blocks. Memories are recalled separately through `ctx recall`.
<!-- ctx:end -->

# Destructive deletion safety

- Never delete a folder or directory without the user's explicit approval, even when deletion appears implied by the task.
- Treat any bulk, recursive, or otherwise broad deletion as risky and require explicit user approval immediately before performing it. This includes commands or tools such as `rm -r`, `rm -rf`, `find -delete`, `git clean`, cleanup scripts, and deleting multiple files at once.
- Before requesting approval, state exactly which paths will be deleted and why. Do not expand the deletion beyond the approved paths.
- When approval has not been given, use non-destructive alternatives where possible, such as moving items to a temporary or quarantine location, or leave them in place and report them to the user.

# Research workflow

- Treat `ctx` as this project's research index and query layer. Do not leave material research only in chat history.
- Write first-party research reports under `docs/research/` and keep observations clearly dated.
- Add each report to ctx as a local notes resource with `ctx add file://<absolute-path> --label <label> --reason <reason> --cwd <repo>`, then link it with `ctx link <label> --cwd <repo>`.
- After editing a linked report, refresh it with `ctx update <label> --force --cwd <repo>` and run `ctx sync --reindex --cwd <repo>`.
- Verify important reports with a label-scoped `ctx query` before relying on them.
- Link authoritative external sources as ctx docs resources so first-party conclusions remain traceable to their evidence.

# Judge.me integration invariants

- Use Bun 1.3.14 for dependency installation and workspace commands. Keep `bun.lock` as the sole lockfile; do not introduce `package-lock.json`, `yarn.lock`, or `pnpm-lock.yaml`.
- Use `bun run test` for the repository test script. Plain `bun test` invokes Bun's test runner directly and skips the package's pre-test build.
- The Hydrogen harness uses `http://localhost:3001` for local development. Keep port 3000 free for the user's other project.
- Treat `JUDGE_ME_WIDGETS` as a coverage roadmap, not an implementation registry. At present only `StarRatingBadge`, `VerifiedReviewsCounter`, `AllReviewsCounter`, `ReviewsCarousel`, `LegacyReviewWidget`, `AllReviewsWidget`, `FloatingReviewsTab`, `ReviewsGrid`, `CardsCarousel`, `TestimonialsCarousel`, `VideosCarousel`, `PopupReviews`, `AiReviewsSummary`, `ReviewSnippets`, and `QuestionsAndAnswers` are implemented. Do not describe the catalog as supported until each entry has a data adapter, public component, tests/fixtures, and browser verification.
- When a route renders both product widgets, use `fetchLegacyProductWidgets` so `settings` and `html_miracle` are requested and serialized once. Reconstruct each complete component payload with the shared `resources` object at render time; do not duplicate the roughly 600-field settings object in loader data.
- When the same route also renders shop widgets, use `fetchLegacyStorefrontWidgets`. It adds `featured_carousel`, `all_reviews_page`, `reviews_tab`, and `verified_badge` while keeping one shared settings/CSS payload. The Verified Reviews Counter, All Reviews Counter, classic carousel, All Reviews Widget, and floating tab are shop-level and must not receive a product `external_id`. Reuse the one `all_reviews_page` response for the aggregate counter and the Free-plan Floating Tab fallback.
- The standalone All Reviews Counter uses public `all_reviews_count` and `all_reviews_rating` reads. In the full storefront batch, derive those values from the existing `all_reviews_page` header so only the separate `verified_badge` read raises the legacy batch to eight requests. Keep Judge.me's `.jdgm-all-reviews-text`, `.jdgm-all-reviews-rating`, and `.jdgm-all-reviews-text__text` markers; `widget/others.js` owns the branded star/verification treatment. Never emit the official `javascript:void(0)` link when no safe dashboard URL exists.
- The Verified Reviews Counter must use exact public `verified_badge` markup and return `null` when Judge.me returns `null`; never estimate it from all reviews. Judge.me requires at least 20 verified reviews. Preserve `verified_count_badge_*` dashboard settings and the current `widget/others.js` branded/classic transformation. The branded SVG needs `judgeme-public-images.imgix.net` in `connectSrc` and `imgSrc`; classic mode needs `s3.amazonaws.com` in `imgSrc`. The 2026-07-13 authorized fixture returned 866 and was verified in Brave.
- Reviews Grid uses the tokenless `cdn.judge.me/widgets/reviews_grid_widget_data` endpoint plus Judge.me's deployment-specific Shopify extension assets. Require `v3AssetBaseUrl` from the host, validate the official `cdn.shopify.com/extensions/.../.../assets/` shape, and never hardcode a supposedly permanent deployment UUID in the library.
- When Reviews Grid shares a route with the legacy storefront batch, fetch only its grid page and combine it with `resources.settings` plus `allReviewsCounter` through `createReviewsGridData`. This keeps the route at nine requests rather than repeating settings and aggregate reads.
- Judge.me's v3 Reviews Grid entry auto-scans the document and does not expose its manager. Execute a uniquely queried module for each SPA root, remove `data-entry-point` from completed roots, and reject two shops or extension deployments in one document. The current grid is an exact client mount, not SSR review-card markup.
- Cards Carousel uses one tokenless `cdn.judge.me/reviews/reviews_for_carousel` read plus the deployment's `carousels.css`, `carousels.js`, `media_carousel.js`, `video_carousel.js`, and manifest-resolved `carousel-lightbox/main.js`. Seed `window.jdgmCarouselMode` from the server response so the browser does not repeat the data request.
- The Cards navigation/media script scans only roots present during its evaluation. Execute a uniquely queried `video_carousel.js` for each SPA root, retain its per-root initialization guard, and use React handlers instead of the theme's CSP-unsafe inline `onclick` attributes.
- Judge.me's theme cart mode depends on a Shopify bridge. In headless code, require current cart product IDs and map them to the public `custom_products` carousel read. Keep the returned data labeled as the caller's `cart` selection and never serialize private credentials.
- Testimonials Carousel uses the same tokenless endpoint with `carousel_type=testimonials`, plus `carousels.css`, `carousels.js`, `testimonials_carousel.js`, and the manifest-resolved carousel lightbox. Seed its mode from the server response, keep React-owned CSP-safe arrows, pause autoplay on hover/focus, and clear the interval and delegated lightbox listener on unmount.
- Testimonials block settings live in Shopify's theme editor rather than Judge.me's public settings response. Preserve the typed React configuration for selection/filter, quote/product/reviewer/card sizing, arrows, colors, border/shadow, transition, and header controls. A full product loader with legacy widgets, Reviews Grid, Cards Carousel, and Testimonials Carousel makes eleven Judge.me reads.
- Videos Carousel requires `carousel_type=videos` and `review_type=video|photo_and_video|all`; omitting `review_type` returned HTTP 400 on 2026-07-13. It reuses `carousels.css`, `carousels.js`, `media_carousel.js`, `video_carousel.js`, and the carousel lightbox. Videos-only mode remains hidden below three cards. The current navigation bundle does not set `_initialPositioningComplete` for a normal Videos Carousel; readiness requires the initialized flag, attached carousel instance, and an active media card instead. Release the per-root video player, click listener, and carousel mode on SPA unmount. A full product loader that also includes Videos Carousel makes twelve Judge.me reads.
- The 2026-07-13 fixture has no published video cards. Photo/video mode, navigation, and the image lightbox are Brave-verified; actual iframe playback/autoplay and an exact Shopify Perspective-size comparison still require dedicated fixtures.
- Pop-up Reviews is a native global embed because the core extension manifest has no popup entry and the official second embed is Awesome-plan gated. Read its 18 current dashboard fields from the shared settings payload, map selection to one tokenless `reviews_for_carousel` read, serialize only public display fields, and keep all timers component-scoped. Product-picture mode requires host-supplied Storefront API URLs by product handle; never scrape product pages or expose the private token. A full product loader including the popup makes thirteen Judge.me reads.
- Hydrogen's starter CSS styles every `<aside>` as its viewport-height drawer. Keep the Pop-up Reviews root as a neutral element and retain its explicit inset, height, background, and shadow resets so host drawer CSS cannot pull the popup away from its configured corner.
- AI Reviews Summary production content comes only from `shop.metafields.judgeme.store_summary_widget_data`. The tokenless `store_summary/status` route reports eligibility/generation state but does not return summary content. Return `null` when the metafield is absent; never synthesize a replacement and present it as Judge.me data. The Hydrogen harness may use an unmistakably labeled local fixture for the current Free-plan store.
- AI Reviews Summary block layout/colors live in Shopify theme-editor `data-*` attributes rather than the public Judge.me settings response. Keep the typed block config separate while reusing global localized labels and branding flags from shared `settings`. The current `judgeme-624` entry renders summary text and sentiment keywords but no documented review-highlights field; do not claim highlight support without a verified live schema.
- Judge.me's `store-summary-widget/main.js` entry auto-scans pending roots and exposes no manager. Serialize unique module scans, seed `jdgm.data.storeSummaryWidget` with the validated metafield payload, wait for `.jdgm-widget-revamp .jm-store-summary`, and remove `data-entry-point` from completed roots so SPA scans do not remount them. Adding this widget does not add another Judge.me data read to the full product loader.
- Review Snippets uses the tokenless `api.judge.me/reviews/reviews_for_review_snippet_widget` v2 endpoint plus the manifest-resolved `review-snippet-widget/main.js` module and CSS. The official Shopify block is Awesome-plan gated, but the endpoint currently returns public data for the Free-plan fixture; treat that as a compatibility workaround rather than a guaranteed Judge.me entitlement.
- The current Review Snippets module always performs its own fetch. Register the loader response before evaluating the module and retain the narrowly scoped, reference-counted fetch bridge so only the exact matching GET URL is served from loader data. Release each retained response on React unmount and delegate all other requests to the original browser fetch. A full product loader including Review Snippets makes fourteen Judge.me reads, with no duplicate browser read for snippets.
- Review Snippets product mode defaults to five cards; the current request contract and cart documentation allow up to ten. Preserve the block's selection/filter/layout/color attributes, validate returned `body_html`, keep empty feeds unmounted, and let Judge.me's current module own arrows, automatic movement, and the v3 review lightbox.
- Questions & Answers is officially an Awesome-only tab/form inside the new Review Widget, not a standalone Shopify block. The headless adapter is native React and uses tokenless `GET api.judge.me/api/questions/questions_for_widget` plus tokenless multipart `POST api.judge.me/api/questions`, while reusing shared dashboard settings. A complete product loader including Q&A and the Verified Reviews Counter makes fifteen Judge.me reads.
- Keep the Q&A POST CORS-simple: let `FormData` set `Content-Type`, add no non-safelisted headers, and validate required shopper/product fields before sending. The current route returns CORS on multipart POST responses but HTTP 404 to `OPTIONS`, and even an empty diagnostic POST returned HTTP 200 `{}`. Never treat that empty response as proof of queueing or add either Judge.me token.
- The 2026-07-13 fixture has Q&A disabled and zero published questions. Its Hydrogen demo uses Judge.me's `preview_mode=sample_data`, labels it as read-only sample content, disables submission, and suppresses sample pagination because page two repeats the same semantic questions under new UUIDs. Publication/moderation and live pagination still require a seeded Awesome-plan fixture.
- Judge.me's `reviews_tab` endpoint returns `null` when the Awesome-only Floating Reviews Tab is unavailable. The adapter falls back to `all_reviews_page`, which remains public on the current Free-plan store. An eligible store can return only `.jdgm-revs-tab__wrapper`, without the `.jdgm-revs-tab` root/trigger normally supplied by the Shopify app embed; normalize that exact response with the configured shell while preserving complete exact roots unchanged. Keep the exact response path and the fallback path separate; the fallback's product/store tabs, pagination, rating filters, and sorting must use public Widget API reads only.
- Judge.me's secondary bundle closes over the Floating Reviews Tab roots present on its first document-ready pass and can clone/replace the `.jdgm-revs-tab` subtree after React binds it. A remounted SPA root therefore needs React-owned open/close and Escape listeners. Delegate tab-button, close-button, mask, and fallback-control events from the stable outer React container, resolve the current Judge.me root at event time, release the body scroll lock on unmount, and intercept fallback controls before Judge.me tries the unavailable `reviews_tab` endpoint.
- React development Strict Mode can overlap two asynchronous Floating Reviews Tab initializations. Check that the originating effect is still current immediately before binding; a stale initializer must neither replace the current disposer nor dispose the current listener set after it resolves.
- Judge.me's `loader.js` maps `.jdgm-all-reviews-widget` to `widget/arp.js`. That bundle can replace subtab markup after its initializer becomes available and normally binds cache-server paging. Keep React-owned capture listeners for tabs, filters, sorting, and pagination, and reapply keyboard semantics after CDN DOM mutations.
- Do not use `all_reviews_header[data-per-page]` as the direct Widget API page size. On the 2026-07-13 test store it says 10 while `all_reviews_page` returns 25 reviews per full page. Record the largest observed full-page count, raise it when another stream returns a larger page, and also compare the accumulated DOM count with the header's product/shop totals to detect the final page.
- Judge.me's public `product_review` endpoint returns legacy DOM even when dashboard settings say the new Review Widget is enabled. The legacy adapter must set `review_widget_revamp_enabled` to `false`; removing that override makes the current loader skip the returned markup.
- Judge.me's browser runtime mutates `window.jdgm`, `window.jdgmSettings`, and widget DOM. Load its script once per document, preserve its enriched settings across SPA navigation/HMR, and do not initialize two Shopify stores in one document.
- Judge.me may prefetch `widget/form.js` and `widget/media.js` without executing them. Before manually calling `initializeWidgets` for a Review Widget inserted by SPA navigation, explicitly request those scripts and their CSS, then wait for the form/media initializer functions. A same-product Home/back Brave cycle must leave all mounted adapters at `ready`.
- Keep Judge.me-owned SSR markup outside streamed `Suspense` until the runtime/hydration race has a tested lifecycle solution. The current Hydrogen route intentionally awaits the widget payload before returning loader data.
- The consuming app owns CSP. A custom Hydrogen `scriptSrc` must retain `'self'`; local Vite requires `workerSrc: ["'self'", 'blob:']`. Keep `blob:` out of `scriptSrc`, and update the host allowlist when a Judge.me interaction introduces a new CDN/API/media/frame origin. Judge.me's rebranded verification helper fetches `https://judgeme-public-images.imgix.net/judgeme/logos/verified-checkmark.svg`, so that host must be present in both `connectSrc` and `imgSrc`. Exact v3 extension adapters require `https://cdn.shopify.com` in `scriptSrc`, `styleSrc`, `connectSrc`, `fontSrc`, and `imgSrc`.
- Judge.me may create `javascript:void(0)` links after initialization. Keep the component-scoped mutation sanitizer unless the runtime stops emitting them or the links are replaced with an equivalent CSP-safe event contract.
- The public Widget API token may be used for public reads. Never serialize the private token through loaders, providers, browser bundles, logs, fixtures, ctx, or committed files.
- Run `bun run test`, `bun run lint`, `bun run typecheck`, and `bun run build` for code changes. For runtime/CSP changes, also do a clean Brave product-page reload and exercise at least one interactive path; static checks cannot validate Judge.me's current third-party deployment.
